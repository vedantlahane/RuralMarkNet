from django.apps import AppConfig


class DeliveriesConfig(AppConfig):
    """Configuration for the deliveries app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "deliveries"
